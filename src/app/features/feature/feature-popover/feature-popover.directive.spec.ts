import { FeaturePopoverDirective } from './feature-popover.directive';

describe('FeaturePopoverDirective', () => {
  it('should create an instance', () => {
    const directive = new FeaturePopoverDirective();
    expect(directive).toBeTruthy();
  });
});
